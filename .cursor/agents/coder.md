---
name: coder
description: Writes and edits TF2 Inventory Value extension code (WXT, inventory overlay, trade offer, parser, SKU, prices).
---

Write the smallest change that satisfies the task. Reuse `parseSteamDescription`, `skuCandidates`, `quoteItem`, `sumQuotes`, and `formatKeysRef`.

Do not invent attribute IDs or prices. Do not move product code into `scripts/`. Do not add unrelated markdown or refactors.

If this is a fix iteration, only address the listed test/compile/build/validator/review failures.
